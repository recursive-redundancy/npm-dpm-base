const binary = require('./binary.js');
const decimal = require('./decimal.js');
const hex = require('./hex.js');
const octal = require('./octal.js');

console.log('main');

module.exports = {
  binary,
  decimal,
  hex,
  octal
}
